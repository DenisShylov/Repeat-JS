const search = document.querySelector('.search__input');
const searchBtn = document.querySelector('.search__btn');

const test = () => console.log(search.value);
searchBtn.addEventListener('click', test);
